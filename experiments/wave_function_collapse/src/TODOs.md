TODOs:
* look into Image memory management. Am I storing whole images in structs or just header info?
* update the texture in the texture array, instead of the base diffuse texture, on pressing space
* add the 2nd texture and instance only when pressing some button
* handle disparate texture sizes
* fill in function for re-sizing texture array
* add function to add a new instace and reference a region of an existing texture or to add
  a texture to the texture array and reference a region of that new texture.